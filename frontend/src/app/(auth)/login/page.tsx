import Template from "@/app/Template";
import LoginForm from "@/components/loginForm";

const Login = () => {
  return (
    <Template>
      <div>
        <LoginForm />
      </div>
    </Template>
  );
};

export default Login;
